'use strict';

let nimals = ['kuty', 'macsk', 'cic'];

function adder(element) {
    console.log(element + 'a');
}
nimals.forEach(adder);

// nimals = nimals.map(i => i + 'a');
// console.log(nimals);